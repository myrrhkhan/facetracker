cmake_minimum_required(VERSION 3.10)
project(camstitcher)
find_package(OpenCV REQUIRED)
add_executable(camstitcher main.cpp get_cameras.cpp get_cameras.hpp)
target_link_libraries(camstitcher ${OpenCV_LIBS})
